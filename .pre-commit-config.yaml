repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: gitleaks (staged)
        entry: scripts/gitleaks-pre-commit.sh
        language: unsupported_script
        pass_filenames: false
        always_run: true
        require_serial: true
        stages: [pre-commit]
